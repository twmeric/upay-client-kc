{
  "name": "payment-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for EasyLink Payment Gateway",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:create": "wrangler d1 create payment-db",
    "db:migrate": "wrangler d1 execute payment-db --file=./schema.sql"
  },
  "keywords": ["cloudflare", "worker", "payment", "easylink"],
  "author": "",
  "license": "MIT"
}
